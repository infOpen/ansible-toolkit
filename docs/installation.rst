.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install ansible_toolkit

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ansible_toolkit
    $ pip install ansible_toolkit
